namespace DirectN.Extensions.Utilities;

public enum WindowResizedType
{
    Restored = (int)Constants.SIZE_RESTORED,
    Minimized = (int)Constants.SIZE_MINIMIZED,
    Maximized = (int)Constants.SIZE_MAXIMIZED,
    MaxShow = (int)Constants.SIZE_MAXSHOW,
    MaxHide = (int)Constants.SIZE_MAXHIDE,
}
